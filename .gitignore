# Godot engine artefacts
.godot/
/android/
*.translation

# Local toolchain: engine binary, editor/user data, test output.
# Provisioned by tools/ci/bootstrap_env.sh; never committed.
.tooling/

# Export output
/build/
/export/

# Tool output: validator reports, promotion contact sheets. Regenerated on
# every CI run; see docs/PART_DATA_SCHEMA.md §14 and docs/EXTENSION_PIPELINE.md §8.
/.build/

# DCC scratch that escaped art_src/
*.blend1
*.blend2

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*~
